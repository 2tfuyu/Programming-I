#include <stdio.h>

int main(void)
{
	int x, y;
	int ans = 1;
	
	while (ans % 5 != 0) {
		printf("®”1=");
		scanf("%d", &x);
	
		printf("®”2=");
		scanf("%d", &y);
	
		ans = x + y;
	}
	return (0);
}